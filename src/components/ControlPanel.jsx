import * as React from "react";
import { Container, Sidebar } from "semantic-ui-react";

export const ControlPanel = () => (
    <Sidebar
        as={Container}
        visible={true}
        direction="right"
    >
        Hello
    </Sidebar>
)
