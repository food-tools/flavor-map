const initialState = {
    scale: 100,
    translate: [0, 0]
};

export const view = (state=initialState, action) => {
    switch (action.type) {
        default: {
            return state;
        }
    }
}
